# pillow
# 打开图片 
# 导入PIL模块
import PIL
# 从PIL库中导入Image
from PIL import Image 
# 使用open()方法打开路径"/Users/qian/heart.png"中的图片
# 赋值给变量img
img = Image.open("/Users/qian/heart.png")

# 将图片转化为二值图片
用Image.open()打开图片时，返回的对象模式都是"RGB"。

由于RGB模式的图片实际上是由三个通道编码构成，而我们需要单通道的编码格式。

所以需要使用convert()函数将RGB图片转化单通道的二值图像。

+ RGB模式图片
RGB模式的图像有3个颜色通道，分别为红(Red)，绿(Green)和蓝(Blue)

RGB模式的图片是三个通道的图片通过特定的编码格式叠加之后形成的

# 模式转换
## convert()模式转化
+ convert()函数，作用是将RGB模式的图像转化为其他模式的图像。

九种不同模式如下：1，L，P，RGB，RGBA，CMYK，YCbCr，I，F。
<img width="817" height="850" alt="wecom-temp-f31b860af4a2cb0d1654fa3af3baeba8" src="https://github.com/user-attachments/assets/6f5fdcc8-02ae-40c6-aeb8-fb0cdfba52b4" />

```
# 使用convert()方法将图片转化为二值图片
# 赋值给变量img_1
img_1=img.convert("1")
```
"1"
参数1，代表选用模式1，作用是将图片转化为二值图片。

由于计算机采用二进制编码，bit是二进制编码中表示信息的最小单位，每个bit只有"0"和"1"两个取值，因而n个bit的信息量可以表现出2的n次方种选择。

二值图像的每个像素用8个bit表示，所以8个bit构成的像素取值范围为0~255。

又因为二值图片只有黑、白两种颜色，所以每个像素值为0时表示该像素点为黑色，255表示该像素点为白色。

## 保存图片
```python
# 使用save()函数将图片保存到路径"/Users/qian/new-heart.png"
img_1.save("/Users/qian/new-heart.png")
```

## 调整图片大小
在进行编码前，我们还需要调整图片大小。

因为图片越大，像素点越密，生成图片的密度也就更大。
+ resize()
```python
# 把图像的size重新设定为(20,20)，也就是宽20像素，长20像素
 # 赋值给变量new_img
new_img = img_1.resize((20,20))
```

## 嵌套列表
+ 列表中的元素也可以是列表，而且列表中元素的数量可以是不同的。

我们常常用列表的长度描述列表中元素的数量
  + 我们可以用len()函数来计算列表长度

+ 生成嵌套列表
我们可以依次读取每一个点的像素值，并将一行的数值都存储到一个列表中。

这样依次读取，直到读完全部20行，20列并将所有数值都存储到新列表中，就生成了嵌套列表。
<img width="2953" height="1377" alt="16-P24" src="https://github.com/user-attachments/assets/1f6ba98a-c13c-45d7-9ee2-e53ce9e00040" />

## 嵌套循环
先内循环，后外循环，不断迭代
<img width="1369" height="679" alt="屏幕截图_22-8-2026_19298_np baicizhan com" src="https://github.com/user-attachments/assets/a540302d-14b7-4ff0-bd46-581854a42bc5" />

## 存储单行像素值
```python
# 使用for循环遍历0到19
for i in range(20):
    # 定义一个空列表row存储一行中每一列的值
    row = []
    # 使用for循环遍历0到19
    for j in range(20):
        # 使用getpixel方法获取图像中某一点像素的颜色值
        pix = new_img.getpixel((j,i))
        # 将像素值依次存储到列表row中
        row.append(pix)
        # 依次打印坐标点像素
        print(row)
```

现在生成的列表是0，255样式，我们可以通过取模运算，转化为0，1的嵌套列表形式，看上去更清爽。

PS：因为数字0，1只用于表示，对应像素点位是否粘贴图片，因此数值的设置并不影响最终效果。实际使用中不做任何转换也没有问题哦

```python
# 使用for循环遍历0到19
for i in range(20):
    # 定义一个空列表row存储一行中每一列的值，0或1
    row = []
    # 使用for循环遍历0到19
    for j in range(20):
        # 使用getpixel方法获取图像中某一点像素的颜色值
        pix = new_img.getpixel((j,i))
        # 通过取模运算，把颜色值255转化为1
        pix_new = pix % 254
        # 将像素值依次存储到列表row中
        row.append(pix_new)
    # 依次打印坐标点像素
    print(row)
    # 将每一行值存储在列表figure中      
    figure.append(row)
```

# 制作照片墙
## 设置纯色背景
```python
# 设置单张图片的像素，图片宽度pic_width为100像素，长度pic_height为100像素
pic_width = 100
pic_height = 100

# 读取路径为"/Users/qian/blue.png"的背景图片
# 将背景图片的尺寸设置为(2000，2000)
img_back = Image.open("/Users/qian/blue.png").resize((2000,2000))
```

## 读取照片
接下来我们需要读取猫咪文件夹中的照片。

照片存放在路径"/Users/qian/kitten"中。
```python
# 导入os模块
import os
# 获取文件夹"/Users/qian/kitten"内文件名称所组成的列表
image_names = os.listdir("/Users/qian/kitten")
```

## 粘贴照片
接下来我们粘贴照片，方法和上节课中设计嵌套列表的方法是一致的。

这节课中我们同样可以遍历列表的每一行每一列，在数值为0的坐标位置粘贴文件夹中的图片就行啦。

+ 粘贴照片墙步骤

具体步骤如下：

1.遍历列表的每行每列，判断哪个像素点的数值为0。
2.从文件夹中随机选取一张照片。
3.调整图片大小。
4.将图片粘贴到列表中标记为0的位置。

### 选取随机图片
遍历列表的方法和上节课是类似的。

接下来我们完成随机从文件夹kitten中选取一张照片。

我们如何随机选取图片呢？可以使用random随机函数。

+ random.choice()
  + choice()函数，返回一个列表，元组或字符串的随机项。
  + 不能直接访问，需通过random静态对象访问
 
### 拼接图片路径
用"/Users/qian/kitten/"+照片名称拼接，获取每张照片的完整路径，并赋值给ran_photo
```python
# 导入random库
import random
# 获取嵌套列表的行数
row_num = len(figure)
# 获取嵌套列表的列数
column_num = len(figure[0])
# 遍历行和列，判断当数值为0时
for row in range(row_num):
    for column in range(column_num):
        if figure[row][column] == 0:
            # 随机选取文件夹kitten中的一张照片，把图片路径赋值给变量ran_photo
            ran_photo = "/Users/qian/kitten/" + random.choice(image_names)
```

### 设置照片大小
```python
# 导入os模块
import os
# 获取文件夹"/Users/qian/kitten"内文件名称所组成的列表
image_names = os.listdir("/Users/qian/kitten/")

# 导入random库
import random
# 获取嵌套列表的行数
row_num = len(figure)
# 获取嵌套列表的列数
column_num = len(figure[0])
# 遍历行和列，判断当数值为0时
for row in range(row_num):
    for column in range(column_num):
        if figure[row][column] == 0:
            # 随机选取文件夹kitten中的一张照片，把图片路径赋值给变量ran_photo
            ran_photo = "/Users/qian/kitten/" + random.choice(image_names)
            # 打开随机选中的照片，并且将照片的尺寸设置宽度为pic_width，高度为pic_height
            pic = Image.open(ran_photo).resize((pic_width,pic_height))
```

### 粘贴图片
遍历嵌套列表，当数值为0时，选择随机照片粘贴。

#### 粘贴背景
这里我们使用之前设置的背景图片：
img_back

+ paste()函数
  + 一个函数，PIL中的paste()函数的作用为将一张图片覆盖到另一张图片的指定位置去。
  + paste()函数的第一个参数，表示需要粘贴的图片
  + paste()函数的第二个参数，表示函数的坐标位置
     + 这里对应的坐标位置为：
       
          x：宽度*列数(pic_width*column)

          y：图片高度*行数(pic_height*row)。

 ```python
# 参数设置为需要粘贴的图片pic，粘贴的位置图片宽度*列，图片高度*行
            img_back.paste(pic,(pic_width * column,pic_height * row))
# 将图片保存到路径："/Users/qian/kitten.png"中
img_back.save("/Users/qian/kitten.png")
```
+ 如果文件读取出现错误提示".DS_Store"时，记得使用文件删除的方法：

image_names.remove(".DS_Store")(macOS系统)

```python
# 导入PIL库
import PIL
# 从PIL库中导入Image
from PIL import Image 

# 使用open()方法打开路径"/Users/qian/heart.png"中的图片
# 赋值给变量img
img = Image.open("/Users/qian/heart.png") 
# 使用convert()方法将图片转化为二值图片
# 赋值给变量img_1
img_1=img.convert("1") 
 # 把图像的size重新设定为(20,20)，也就是宽20像素，长20像素
 # 赋值给变量new_img
new_img = img_1.resize((20,20))
# 定义一个空列表figure
figure = []
# 使用for循环遍历0到19
for i in range(20):
        # 定义一个空列表row存储一行中每一列的值
        row = []
        # 使用for循环遍历0到19
        for j in range(20):
            # 使用getpixel方法获取图像中某一点像素的颜色值
            pix = new_img.getpixel((j,i))
            # 使用取模运算把颜色值255转化为1
            pix_new = pix % 254
            # 将转化后的值依次存储到列表row中
            row.append(pix_new)
        # 将每一行值存储在列表figure中      
        figure.append(row)
# 设置单张图片的像素，图片宽度pic_width为100像素，长度pic_height为100像素
pic_width = 100
pic_height = 100

# 读取路径为"/Users/qian/blue.png"的背景图片
# 将背景图片的尺寸设置为(2000，2000)
img_back = Image.open("/Users/qian/blue.png").resize((2000,2000))
# 导入os模块
import os
# 获取文件夹"/Users/qian/kitten"内文件名称所组成的列表
image_names = os.listdir("/Users/qian/kitten/")

# 导入random库
import random
# 获取嵌套列表的行数
row_num = len(figure)
# 获取嵌套列表的列数
column_num = len(figure[0])
# 遍历行和列，判断当数值为0时
for row in range(20):
    for column in range(20):
        if figure[row][column] == 0:
            # 随机选取文件夹kitten中的一张照片，把图片路径赋值给变量ran_photo
            ran_photo = "/Users/qian/kitten/" + random.choice(image_names)
            # 打开随机选中的照片，并且将照片的尺寸设置宽度为pic_width，高度为pic_height
            pic = Image.open(ran_photo).resize((pic_width,pic_height))
            # 使用paste()函数将选中的照片依次粘贴到照片背景img_back中
            # 参数设置为需要粘贴的图片pic，粘贴的位置图片宽度*列，图片高度*行
            img_back.paste(pic,(pic_width * column,pic_height * row))
```

# PIL.Image常用函数总结
<img width="1086" height="1284" alt="PIL Image" src="https://github.com/user-attachments/assets/a7be3431-28ee-4e96-b2d0-a44f48abd81a" />


# 绘制其他墙

```python
# 导入PIL模块和Image类
import PIL
from PIL import Image

# 读取路径为"/Users/yequ/tool.png"的图片
# 将其转化二值图片，调整大小为(50,50)
img = Image.open("/Users/yequ/tool.png")
img_1=img.convert("1")
new_img = img_1.resize((50,50))

# 设计TOOL图形编码并存储到列表figure中
figure = []
for i in range(50):
    row = []
    for j in range(50):
        pix = new_img.getpixel((j,i))
        pix_new = pix % 254
        row.append(pix_new)
    figure.append(row)

# TODO 设置图片宽度和高度都为100
pic_width=100
pic_height=100

# TODO 设置背景图片和大小，图片路径为
# 背景大小设置为(5000,5000)
img_back=Image.open("/Users/yequ/blue.png").resize((5000,5000))

# TODO 导入os模块
# 获取文件夹内文件名称所组成的列表
import os
image_names=os.listdir("/Users/yequ/xiaoye")


# TODO 定义行数和列数
row_num=len(figure)
column_num=len(figure[0])

# TODO 导入随机函数
import random

# TODO 随机粘贴图片
for row in range(row_num):
    for column in range(column_num):
        if figure[row][column]==0:
            ran_photo="/Users/yequ/xiaoye/" + random.choice(image_names)
            pic=Image.open(ran_photo).resize((pic_width,pic_height))
            img_back.paste(pic,(pic_width*column,pic_height*row))
             





# TODO 保存图片到路径             
img_back.save("/Users/yequ/card.png")
```
+ 绘制效果：
<img width="5000" height="5000" alt="card" src="https://github.com/user-attachments/assets/20bf43fb-9a6a-498e-98d6-12804c0de6ea" />

### 获取新页面中的占位符编号和类型。

1.从pptx模块中导入Presentation；
2.读取文件路径"/Users/yequ/pass.pptx"赋值给变量pptxFile；
3.获取母版中的第二个版式；
4.新建幻灯片页，并赋值给变量newSlide；
5.访问newSlide中的.placeholders，赋值给变量all_plh；
6.格式化输出占位符的编号和类型。
```python
# 从pptx导入Presentation
from pptx import Presentation

# .pptx文件路径赋值给变量path
path = "/Users/yequ/pass.pptx"
# 读取PPT赋值给pptxFile
pptxFile = Presentation(path)
# 获取母版中第2个版式，赋值给变量layout
layout = pptxFile.slide_layouts[1]
# 访问pptxFile中.slides属性，赋值给变量slideAll
slideAll = pptxFile.slides
# 调用add_slide()函数传入参数layout
# 新建幻灯片页，并赋值给变量newSlide
newSlide = slideAll.add_slide(layout)
# 访问newSlide中的.placeholders，赋值给变量all_plh
all_plh = newSlide.placeholders
# for循环遍历newSlide中的.placeholders属性
# 赋值给变量single_plh
for single_plh in newSlide.placeholders:
    # 访问占位符对象中的.placeholder_format属性
    # 赋值给变量phf
    phf = single_plh.placeholder_format
    # 访问phf中的.idx和.type属性
    # 格式化字符串以"-"连接并输出
    print(f"{phf.idx}-{phf.type}")
```

### 填充姓名和图片。
填充姓名支持自定义输入功能
```python
# 在文字占位符中写入name
all_plh[2].text = name

# 将图片赋值给变量picPath
picPath = "/Users/yequ/card.png"
# 在图片占位符中传入图片
all_plh[1].insert_picture(picPath)

# 保存ppxFile
pptxFile.save("/Users/yequ/final_card.pptx")
```
### 保存pptx
+ pptx转图片链接
  
需要下载PPTX文件导成图片，并生成链接：

1.将pptx保存到本地后，打开pptx并截图保存；
2.打开自己的常用邮箱，以qq邮箱为例；
3.上传附件，点击右键保存图片URL；
4.制作图片二维码，word参数设置为图片URL；
```python
# 导入myqr模块
from MyQR import myqr

# 设置二维码展示图片和链接
myqr.run(words="http://nocturne.bczcdn.com/img/1639798054187_5423/card.png",
         save_name="yequ.gif",
         picture="/Users/yequ/happy.gif",
         colorized=True,
         version=6)
```
