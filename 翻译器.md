# HTTP请求
当浏览器向服务器发送一个 HTTP 请求时，包含请求行、请求头、请求体三个部分。

1. 请求行：包含请求方法、请求URL 、HTTP版本。
2. 请求头：服务器辨析请求头获取浏览器的信息。
3. 请求体：承载请求参数的数据。

# HTTP请求方法
HTTP 请求方法也被叫做“请求动作”，服务器会根据不同的请求方法做不同的响应。

+ 常见的请求方法有两种：GET 和 POST。
1. GET - 从指定的服务器请求数据。
2. POST - 向指定的服务器提交要被处理的数据。

# HTTP响应
服务器接收并处理浏览器发过来的请求后，会返回 HTTP 响应，也由三个部分组成，分别是：响应行、响应头、响应正文。

1. 响应行：包含协议版本、状态码。
2. 响应头：包含服务器名称、响应正文类型、响应正文编码等信息。
3. 响应正文：服务器返回浏览器的数据信息

# HTTP状态码
HTTP 状态码（Status）是服务器告诉浏览器当前请求响应的状态，也就是服务器告诉浏览器：发生了什么事。

+ HTTP状态码由 3 位数字构成的，以下是常见状态码：

200 - 请求成功

301 - 请求的网页被分配了新的URL

404 - 请求的网页不存在

500 - 服务器错误

<img width="866" height="480" alt="pac-10" src="https://github.com/user-attachments/assets/655e9562-9292-410e-9cfc-3e401a3ab287" />

+ requests.get() 函数

# URL链接
网页链接需要作为参数传入。

在本例中，将网页链接以字符串格式赋值给变量 url，再将变量 url 作为参数传入 `requests.get()` 函数中。
```python
import requests
url = "https://nocturne-spider.baicizhan.com"
response = requests.get(url)
print(response)

# 使用.text属性获取网页前1000个字符的内容，并赋值给content
content = response.text[:1000]

# print()输出content
print(content)
```

# 选取翻译服务
在开始制作翻译工具前，我们现在来看看提供翻译服务的网页。
+ 仔细观察主页面，除了在线翻译，还提供了其他的功能，而我们要使用的是文本翻译功能，就需要找到指定的应用程序接口（API）。
+ 也就是说，我们可以通过爬虫去请求指定的 API，从而获取翻译数据。

# 应用程序接口
## 发送POST请求
+ API 以字符串格式赋值给变量。在本例中 API 赋值给变量 url
+ 请求数据
  
requests模块支持将指定数据发送 POST 请求，需要将请求的数据构成一个字典。

在本例中，我们要向服务器请求翻译“夜曲”，请求翻译的数据格式为：

{"i":"夜曲", "doctype":"json"}
***
+ requests.post()函数
  
requests.post() 函数将 POST 请求发送到指定的服务器，并且返回一个包含服务器资源的 Response 对象；
***
+ 传入参数

+ 向 requests.post() 函数中传入链接，**指定请求的服务器**。

  + 在本例中传入变量 url 到 requests.post() 函数中
+ 参数data

向 requests.post() 函数中传入参数data，作为 POST **请求的数据**。

在本例中将变量 word 作为 data 的参数值。

# 反反爬虫策略——设置User-Agent
+ 声明“我是谁”的就是 HTTP 请求头部的 User-Agent（用户代理）
+ user-Agent 在Header里找

## 设置UA
新加一个字典 header，键为User-Agent
```python
import requests
url = "https://fanyi.youdao.com/translate"
word = {"i":"夜曲", "doctype":"json"}
header = {"User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_14_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/84.0.4147.89 Safari/537.36"}
response = requests.post(url, data=word,headers=header)
```

# JSON格式
JSON 的全称为 JavaScript Object Notation，是一种轻量级资料交换格式。

简单地说，JSON 可以将 JavaScript 对象中表示的一组数据转换为字符串，然后就可以在网络或者程序之间轻松地传递这个字符串。

+ JSON 语法很简便，满足以下规则：

1. 数据在键值对中；
2. 数据间由逗号分隔；
3. 大括号 {} 保存对象；
4. 中括号 [] 保存数组，数组可以包含多个对象。

在 JSON 中，对象指的是一个键值对集合。一个对象以 { 开始，以 } 结束。

+ 当我们获取到 JSON 数据后，可以进行解码，转换成字典数据类型，从而提取出指定数据。

# 解码JSON数据
+ 将 JSON 字符串作为参数传入 json.loads() 函数中，可以把 JSON **字符串**转换成**字典**。
+ json.loads() 函数转换后的字典，可以通过索引键，找到对应的值。

+ 通过JSON解码，我们得到了一个字典，想要从中提取出"Nocturne"，就需要字典层层索引，找到对应的值。
1. 第一层：
data["translateResult"]
2. 第二层：
data["translateResult"][0]
3. 第三层：
data["translateResult"][0][0]
4. 第四层：
data["translateResult"][0][0]["tgt"]

<img width="1644" height="1220" alt="20-P21" src="https://github.com/user-attachments/assets/ba9758a9-84c5-46a5-97bb-151554273a1f" />

```python
# 使用import导入requests模块
import requests
# 使用import导入json模块
import json

# 将API链接赋值给url
url = "https://fanyi.youdao.com/translate"
# 将User-Agent以字典键对形式赋值给header
header = {"User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_14_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/84.0.4147.89 Safari/537.36"}
# 构建一个字典word，添加键值为"i"其值为"夜曲"
# 添加键值为"doctype"其值为"json"。
word = {"i":"夜曲", "doctype":"json"}
# 使用requests.post()，赋值给变量response
# 传入url
# 传入参数data，值为字典word
# 传入参数headers，值为字典header
response = requests.post(url, data=word, headers=header)
# 使用.text获取内容并赋值给res_text
res_text = response.text
# 使用json.loads()将str转换成dict，赋值给data
data = json.loads(res_text)
# 通过字典层层索引，找到翻译后的内容，并赋值给translate
translate = data["translateResult"][0][0]["tgt"]
# print()输出translate
print(translate)
```

代码运行成功后输出了"nocturnes"，这样，我们就实现了论文翻译工具中的核心功能，yes！

接下来，我们要读取 Word 文档，将文档中的文本按段落提取并实现批量翻译，再把翻译后的内容放在新的 Word 文档中。

# 读取Word文档
```python
# 使用import导入docx模块
import docx

# 将Word文档路径赋值给变量path
path = "/yequ/论文.docx"
# 读取Word文档赋值给变量docxFile
docxFile = docx.Document(path)
# 获取全部段落赋值给变量paragraphs
paragraphs = docxFile.paragraphs
```
+ 在这里，我们使用 for 循环遍历段落列表，在段落对象中访问 .text 属性，提取出非空段落，并发送 POST 请求，得到翻译结果
```python
# for循环遍历paragraphs，并赋值给par
for par in paragraphs:
    # 使用.text获取段落文本并赋值给par_text
    par_text = par.text
    # 如果段落文本不为空
    if par_text != "":
        # 构建一个字典word，添加键值为"i"其值为par_text
        # 添加键值为"doctype"其值为"json"。
        word = {"i":par_text, "doctype":"json"}
        # 使用requests.post()，赋值给变量response
        # 传入url
        # 传入参数data，值为字典word
        # 传入参数headers，值为字典header
        response = requests.post(url, data=word, headers=header)
        # 使用.text获取内容并赋值给res_text
        res_text = response.text
        # 使用json.loads()将str转换成dict，赋值给data
        data = json.loads(res_text)
        # 通过字典层层索引，找到翻译后的内容，并赋值给translate
        translate = data["translateResult"][0][0]["tgt"]
        # print()输出translate
        print(translate)
```
+ 在这里，我们可以设置每次访问的间隔时间，来规避短时间内频繁访问的问题。

也就是说，发送一次 POST 请求后，停顿一段时间，再进行下一次请求。

```python
# 使用import导入requests模块
import requests
# 使用import导入json模块
import json
# 使用import导入docx模块
import docx
# 使用import导入time模块
import time

# 将Word文档路径赋值给变量path
path = "/yequ/论文.docx"
# 读取Word文档赋值给变量docxFile
docxFile = docx.Document(path)
# 获取全部段落赋值给变量paragraphs
paragraphs = docxFile.paragraphs
# 新建Word文档赋值给变量docxNew
docxNew = docx.Document()

# 将API链接赋值给url
url = "https://fanyi.youdao.com/translate"
# 将User-Agent以字典键对形式赋值给header
header = {"User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_14_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/84.0.4147.89 Safari/537.36"}

# for循环遍历paragraphs，并赋值给par
for par in paragraphs:
    # 使用.text获取段落文本并赋值给par_text
    par_text = par.text
    # 如果段落文本不为空
    if par_text != "":
        # 构建一个字典word，添加键值为"i"其值为par_text
        # 添加键值为"doctype"其值为"json"。
        word = {"i":par_text, "doctype":"json"}
        # 停顿1秒
        time.sleep(1)
        # 使用requests.post()，赋值给变量response
        # 传入url
        # 传入参数data，值为字典word
        # 传入参数headers，值为字典header
        response = requests.post(url, data=word, headers=header)
        # 使用.text获取内容并赋值给res_text
        res_text = response.text
        # 使用json.loads()将str转换成dict，赋值给data
        data = json.loads(res_text)
        # 通过字典层层索引，找到翻译后的内容，并赋值给translate
        translate = data["translateResult"][0][0]["tgt"]
        # 使用add_paragraph()向文档中添加translate
        docxNew.add_paragraph(translate)
        
# 保存并命名文档至指定路径
docxNew.save("/yequ/翻译论文.docx")
```
