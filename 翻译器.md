# HTTP请求
当浏览器向服务器发送一个 HTTP 请求时，包含请求行、请求头、请求体三个部分。

1. 请求行：包含请求方法、请求URL 、HTTP版本。
2. 请求头：服务器辨析请求头获取浏览器的信息。
3. 请求体：承载请求参数的数据。

# HTTP请求方法
HTTP 请求方法也被叫做“请求动作”，服务器会根据不同的请求方法做不同的响应。

+ 常见的请求方法有两种：GET 和 POST。
1. GET - 从指定的服务器请求数据。
2. POST - 向指定的服务器提交要被处理的数据。

# HTTP响应
服务器接收并处理浏览器发过来的请求后，会返回 HTTP 响应，也由三个部分组成，分别是：响应行、响应头、响应正文。

1. 响应行：包含协议版本、状态码。
2. 响应头：包含服务器名称、响应正文类型、响应正文编码等信息。
3. 响应正文：服务器返回浏览器的数据信息

# HTTP状态码
HTTP 状态码（Status）是服务器告诉浏览器当前请求响应的状态，也就是服务器告诉浏览器：发生了什么事。

+ HTTP状态码由 3 位数字构成的，以下是常见状态码：

200 - 请求成功

301 - 请求的网页被分配了新的URL

404 - 请求的网页不存在

500 - 服务器错误

<img width="866" height="480" alt="pac-10" src="https://github.com/user-attachments/assets/655e9562-9292-410e-9cfc-3e401a3ab287" />

+ requests.get() 函数

# URL链接
网页链接需要作为参数传入。

在本例中，将网页链接以字符串格式赋值给变量 url，再将变量 url 作为参数传入 `requests.get()` 函数中。
```python
import requests
url = "https://nocturne-spider.baicizhan.com"
response = requests.get(url)
print(response)

# 使用.text属性获取网页前1000个字符的内容，并赋值给content
content = response.text[:1000]

# print()输出content
print(content)
```

# 选取翻译服务
在开始制作翻译工具前，我们现在来看看提供翻译服务的网页。
+ 仔细观察主页面，除了在线翻译，还提供了其他的功能，而我们要使用的是文本翻译功能，就需要找到指定的应用程序接口（API）。
+ 也就是说，我们可以通过爬虫去请求指定的 API，从而获取翻译数据。

# 应用程序接口
## 发送POST请求
+ API 以字符串格式赋值给变量。在本例中 API 赋值给变量 url
+ 请求数据
  
requests模块支持将指定数据发送 POST 请求，需要将请求的数据构成一个字典。

在本例中，我们要向服务器请求翻译“夜曲”，请求翻译的数据格式为：

{"i":"夜曲", "doctype":"json"}
***
+ requests.post()函数
  
requests.post() 函数将 POST 请求发送到指定的服务器，并且返回一个包含服务器资源的 Response 对象；
***
+ 传入参数

+ 向 requests.post() 函数中传入链接，**指定请求的服务器**。

  + 在本例中传入变量 url 到 requests.post() 函数中
+ 参数data

向 requests.post() 函数中传入参数data，作为 POST **请求的数据**。

在本例中将变量 word 作为 data 的参数值。
