# PyAutoGui
+ 定义:
PyAutoGui是一个纯Python的GUI自动化第三方模块，其目的是可以用程序自动控制鼠标和键盘操作，并且多个平台支持，包括Windows, OS X, Linux。

完成点击按钮、填写表单等操作，可以说是解放人工的自动化神器。

# 用程序自动打开微信桌面版
在使用自动化工具前，我们需要准备两个步骤：

1.确定桌面微信已经是已经登陆的状态。
2.设置快捷键。

## 设置快捷键
1.打开桌面版微信
2.点击设置
3.选择快捷键
4.记住当前默认快捷键或者重新设置快捷键

macOS 或 OS X中：
默认开启桌面版微信的快捷键为"command + e"
发送消息的快捷键为"return"

**如果你的电脑是windows系统，在本地运行代码时，需要将"command”修改为“ctrl”，"return"修改为"enter"**

# 打开桌面版微信
+ `hotkey()`
一个函数，作用是操作键盘上的对应按键。
```python
# 导入pyautogui模块
import pyautogui
# 定义一个openWechat()函数来打开微信
def openWechat():
    # 使用hotkey()函数，操作按键"command", "e"
    pyautogui.hotkey("ctrl", "e")
# 调用openWechat()函数打开桌面微信
openWechat()
```

# 查找联系人
完成了打开微信的功能，接下来我们要查找联系人。

我们可以用一个剪贴板来记录微信昵称，用快捷键粘贴昵称并查找对应的微信用户。

~~Python中是否有类似剪贴板的功能呢？接下来我们一起认识pyperclip模块。~~

## pyperclip
pyperclip是一个第三方Python模块，用于**复制和粘贴**剪贴板功能。

pyperclip模块中有两个函数，`copy()`用于向计算机的剪贴板发送文本，`paste()`用于从计算机剪贴板接收文本。

+ 查找联系人的流程：
1.使用快捷键进行搜索
2.从剪贴板复制微信号
3.使用快捷键粘贴微信号
4.使用快捷键确认输入

+ 使用hotkey函数，操作按键"command","f"，打开搜索。

如果你是Windows系统，参数输入("Ctrl","f")

+ 使用pyperclip模块中的copy函数，复制微信号到剪贴板。

参数为name，调用函数时输入微信号。

+ 使用hotkey函数，操作按键"command","v"，粘贴联系人微信号。

如果你是Windows系统，参数输入("ctrl","v")

+ 使用hotkey函数，操作按键"return"，确认搜索。

如果你是Windows系统，参数输入("enter")。

```python
# 导入pyautogui模块
import pyautogui
# 定义一个0penWechat()函数来打开微信
def openWechat():
    # 使用hotkey()函数，操作按键"command", "e"
    pyautogui.hotkey("command", "e")

# 导入pyperclip模块
import pyperclip
# 定义一个查询联系人的函数，参数为name
def chatWho(name):
    # 使用hotkey函数，操作按键"command","f"，打开搜索
    pyautogui.hotkey("command","f")
    # 使用pyperclip模块中的copy函数，复制微信号name到剪贴板
    pyperclip.copy(name)
    # 使用hotkey函数，操作按键"command", "v"，粘贴微信号
    pyautogui.hotkey("command", "v")
    # 使用hotkey函数，操作按键"return"，确认搜索
    pyautogui.hotkey("return")
# 调用OpenWechat()函数打开桌面微信
openWechat() 
# 调用chatWho(name)函数查找联系人，微信号："duqian"
chatWho("duqian")
```

# 发送信息
最后我们来实现发送信息的功能和搜索微信的方式非常类似，流程如下：

1.复制需要发送的信息
2.使用快捷键粘贴
3.按下确认键发送

```python
# 导入pyautogui模块
import pyautogui
# 定义一个0penWechat()函数来打开微信
def openWechat():
    # 使用hotkey()函数，操作按键"command", "e"
    pyautogui.hotkey("command", "e")

# 导入pyperclip模块
import pyperclip
# 定义一个查询联系人的函数，参数为name
def chatWho(name):
    # 使用hotkey函数，操作按键"command","f"，打开搜索
    pyautogui.hotkey("command","f")
    # 使用pyperclip模块中的copy函数，复制微信号name到剪贴板
    pyperclip.copy(name)
    # 使用hotkey函数，操作按键"command", "v"，粘贴微信号
    pyautogui.hotkey("command", "v")
    # 使用hotkey函数，操作按键"return"，确认搜索
    pyautogui.hotkey("return")
    
 # 定义一个发送信息的函数，参数为msg
def sentMsg(msg):
    # 使用pyperclip模块中的copy函数，复制msg到剪贴板
    pyperclip.copy(msg)
    # 使用hotkey函数，操作按键"command", "v"，粘贴msg
    pyautogui.hotkey("command", "v")
    # 使用hotkey函数，操作按键"return"，确认搜发送
    pyautogui.hotkey("return")
    
# 调用OpenWechat()函数打开桌面微信
openWechat() 
# 调用chatWho(name)函数查找联系人，微信号："duqian"
chatWho("duqian")
# 调用sentMsg(msg)函数发送信息
# 信息内容为：今晚9点，一起去软件园老地方吃烤串吧～不见不散～
sentMsg("今晚9点，一起去软件园老地方吃烤串吧～不见不散～")
```

# 优化小工具
如果我们无缝操作太频繁，可能会被微信检测到在使用自动化软件，会有封号危险。

所以在发送微信时需要设置停顿时间。
```python
# 导入pyautogui模块
import pyautogui
# 导入time模块
import time
# 定义一个0penWechat()函数来打开微信
def openWechat():
    # 使用hotkey()函数，操作按键"command", "e"
    pyautogui.hotkey("command", "e")
    # 使用time.sleep()设置停顿1秒
    time.sleep(1)

# 导入pyperclip模块
import pyperclip
# 定义一个查询联系人的函数，参数为name
def chatWho(name):
    # 使用hotkey函数，操作按键"command","f"，打开搜索
    pyautogui.hotkey("command","f")
    # 使用time.sleep()设置停顿1秒
    time.sleep(1)
    # 使用pyperclip模块中的copy函数，复制微信号name到剪贴板
    pyperclip.copy(name)
    # 使用hotkey函数，操作按键"command", "v"，粘贴微信号
    pyautogui.hotkey("command", "v")
    # 使用time.sleep()设置停顿1秒
    time.sleep(1)
    # 使用hotkey函数，操作按键"return"，确认搜索
    pyautogui.hotkey("return")
    # 使用time.sleep()设置停顿1秒
    time.sleep(1)
    
 # 定义一个发送信息的函数，参数为msg
def sentMsg(msg):
    # 使用pyperclip模块中的copy函数，复制msg到剪贴板
    pyperclip.copy(msg)
    # 使用hotkey函数，操作按键"command", "v"，粘贴msg
    pyautogui.hotkey("command", "v")
    # 使用time.sleep()设置停顿1秒
    time.sleep(1)
    # 使用hotkey函数，操作按键"return"，确认搜发送
    pyautogui.hotkey("return")
    
# 调用OpenWechat()函数打开桌面微信
openWechat() 
# 调用chatWho(name)函数查找联系人，微信号："duqian"
chatWho("duqian")
# 调用sentMsg(msg)函数发送信息
# 信息内容为：今晚9点，一起去软件园老地方吃烤串吧～不见不散～
sentMsg("今晚9点，一起去软件园老地方吃烤串吧～不见不散～")
```

# 批量发送
+ 设置列表
```python
# 导入pyautogui模块
import pyautogui
# 导入time模块
import time
# 定义一个0penWechat()函数来打开微信
def openWechat():
    # 使用hotkey()函数，操作按键"command", "e"
    pyautogui.hotkey("command", "e")
    # 使用time.sleep()设置停顿1秒
    time.sleep(1)

# 导入pyperclip模块
import pyperclip
# 定义一个查询联系人的函数，参数为name
def chatWho(name):
    # 使用pyperclip模块中的copy函数，复制name到剪贴板
    pyperclip.copy(name)
    # 使用hotkey函数，操作按键"command","f"，打开搜索
    pyautogui.hotkey("command","f")
    # 使用time.sleep()设置停顿1秒
    time.sleep(1)
    # 使用hotkey函数，操作按键"command", "v"，粘贴微信号
    pyautogui.hotkey("command", "v")
    # 使用time.sleep()设置停顿1秒
    time.sleep(1)
    # 使用hotkey函数，操作按键"enter"，确认搜索
    pyautogui.hotkey("enter")
    # 使用time.sleep()设置停顿1秒
    time.sleep(1)
    
 # 定义一个发送信息的函数，参数为msg
def sentMsg(msg):
    # 使用pyperclip模块中的copy函数，复制msg到剪贴板
    pyperclip.copy(msg)
    # 使用hotkey函数，操作按键"command", "v"，粘贴msg
    pyautogui.hotkey("command", "v")
    # 使用time.sleep()设置停顿1秒
    time.sleep(1)
    # 使用hotkey函数，操作按键"enter"，确认搜发送
    pyautogui.hotkey("enter")

# 定义一个批量发送模版消息的函数
def message(name):
    # 调用打开微信函数 openWechat()
    openWechat()
    # for循环遍历微信号列表
    for i in name:
        # 调用chatWho()函数搜索联系人
        chatWho(i)
        # 等待两秒
        time.sleep(2)
        # 调用sentMsg()函数发送文案
        sentMsg("今晚8点，健身房见哦～")

# 列表存储需要发送微信的联系人微信号
name1 = ["youyisiba","woshicaidan","kaixinjiuhao"]
        
# 调用message()函数批量发送微信，参数为列表名
message(name1)

```
# 向好友发送每日天气状况，暖心关怀～
[中国气象局](https://weather.cma.cn/)
## 通过访问 URL 来获取数据
+ 如何找到对应的 URL 链接呢
<img width="1292" height="546" alt="屏幕截图 2026-08-26 221202" src="https://github.com/user-attachments/assets/4514eaab-2066-4f04-b5fb-2d9189065959" />

<img width="1290" height="574" alt="屏幕截图 2026-08-26 221219" src="https://github.com/user-attachments/assets/29512a06-a122-4157-a8c1-e1c0b7550a7c" />

<img width="1288" height="552" alt="image" src="https://github.com/user-attachments/assets/3ee74905-ed1d-4a6f-9bc1-7a2821461529" />

### 查看服务器返回的数据
<img width="1312" height="576" alt="image" src="https://github.com/user-attachments/assets/82e55a2d-693e-4e33-85aa-ee04e9baea4a" />

## 爬虫获取信息
+ 在开发者工具中，找到请求方式，此处为 GET
```python
# 使用import导入requests模块
import requests

# 将API链接赋值给url
url = "https://weather.cma.cn/api/now/56294"

# 将User-Agent以字典键对形式赋值给header
header = {"User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_14_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/84.0.4147.89 Safari/537.36"}

# 使用requests.get()，赋值给变量response
# 传入url
# 传入参数headers，值为字典header
response = requests.get(url, headers=header)

# print()函数输出变量response
print(response)
```
+ 在返回的 Response 对象中访问 .text 属性，可以获得返回的数据文本。
+ 编写程序向中国气象局的 API 发送了 POST 请求，服务器返回了一串 JSON 格式 数据。
  + 我们想要的具体数据就在 JSON 数据中，想要从中提取出来就要解码 JSON，把字符串转换成字典。
```python
# 使用json.loads()将str转换成dict，赋值给data_json
data_json = json.loads(data)

# print()函数输出data_json
print(data_json)
```

通过 JSON 解码，我们得到了一个字典，想要从中提取出城市，温度，风力，就需要字典层层索引，找到对应的值。

我们以找到温度为例：

第一层：data_json["data"]

第二层：data_json["data"]["now"]

第三层：data_json["data"]["now"]["temperature"]

<img width="818" height="537" alt="22-P24" src="https://github.com/user-attachments/assets/3604870a-bc30-4e1b-97b4-98607b0ba949" />

## 把查天气的功能封装成函数，通过调用函数实现功能。
```python
# 导入pyautogui模块
import pyautogui
# 导入pyperclip模块
import pyperclip
# 导入time模块
import time
# 使用import导入requests模块
import requests
# 使用import导入json模块
import json

# 定义一个openWechat()函数打开微信
def openWechat():
    # 使用hotkey()函数，操作按键"command", "e"
    pyautogui.hotkey("command", "e")
    # 停顿一秒
    time.sleep(1)

# 定义一个查询联系人chatWho()函数，参数为name
def chatWho(name):
    # 使用hotkey函数，操作按键"command","f"，打开搜索
    pyautogui.hotkey("command","f")
    # 停顿一秒
    time.sleep(1)
    # 使用pyperclip模块中的copy函数，复制联系人微信号到剪贴板
    pyperclip.copy(name)
    # 使用hotkey函数，操作按键"command", "v"，粘贴联系人微信号
    pyautogui.hotkey("command", "v")
    # 停顿一秒
    time.sleep(1)
    # 使用hotkey函数，操作按键"return"，确认搜索
    pyautogui.hotkey("return")
    # 停顿一秒
    time.sleep(1)
    
# 定义一个提交信息的sentMsg()函数，参数为msg
def sentMsg(msg):
    # 使用pyperclip模块中的copy函数，复制msg到剪贴板
    pyperclip.copy(msg)
    # 使用hotkey函数，操作按键"command", "v"，粘贴msg
    pyautogui.hotkey("command", "v")
    # 停顿一秒
    time.sleep(1)
    # 使用hotkey函数，操作按键"return"，确认搜发送
    pyautogui.hotkey("return")

# 定义一个getWeather()函数查天气
def getWeather():
    # 将API链接赋值给url
    url = "https://weather.cma.cn/api/now/56294"
    # 将User-Agent以字典键对形式赋值给header
    header = {"User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_14_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/84.0.4147.89 Safari/537.36"}
    # 使用requests.get()，赋值给变量response
    # 传入url
    # 传图参数headers，值为字典header
    response = requests.get(url, headers=header)
    # 访问.text属性获取返回数据文本，并赋值给data
    data = response.text
    # 使用json.loads()将str转换成dict，赋值给data_json
    data_json = json.loads(data)
    # 通过字典层层索引，找到城市，温度，风力，并分别赋值给变量city，temp，wind
    city = data_json["data"]["location"]["name"]
    temp = data_json["data"]["now"]["temperature"]
    wind = data_json["data"]["now"]["windScale"]
    # 返回格式化字符串"亲爱的小夜，现在{city}的天气温度为{temp}度，有{wind}，好好照顾自己"
    return f"亲爱的小夜，现在{city}的天气温度为{temp}度，有{wind}，好好照顾自己"

    
# 调用openWechat()函数打开桌面微信
openWechat() 
# 调用chatWho(name)函数查找联系人"小夜yyds"
chatWho("小夜yyds")
# 信息内容为getWeather()，赋值给weather
weather = getWeather()
# 调用sentMsg(msg)函数发送信息
sentMsg(weather)

```
# 发送数据表
+ 向 row_values() 函数中传入 -1，索引**倒数**第一行
+ 切片
```python
# 导入xlrd模块
import xlrd
# 将Excel文件路径/Users/qian/每日数据.xlsx，赋值给变量path
path = "/Users/qian/每日数据.xlsx"
# 读取path的工作簿并赋值给变量data
data = xlrd.open_workbook(path)
# 使用sheets()获取工作表对象，索引第一个元素赋值给变量table
table = data.sheets()[0]
# 获取最后一行数据,并赋值给列表lastRow
lastRow = table.row_values(-1)
# 对列表lastRow进行切片，获取第二个到最后一个元素，并赋值给saleData
saleData = lastRow[1:]
# print()输出saleData
print(saleData)
```

## 数据处理
### 剔除字段行（段首）
由于工作表的第一行数据为字段，不需要涵盖在计算中，所以在提取时需要剔除掉。
+ 运用切片剔除
```python
# 读取工作表第2列非表头数据，赋值给变量allSales
allSales = table.col_values(1)[1:]
```
### 转化为百分比格式
如何将计算结果转换成百分比格式呢？在这里，我们可以使用 format() 函数。
+ `format()`
str.`format()` 方法通过字符串中的**大括号 {} 来识别替换内容**，从而完成**字符串的格式化**
+ 例如：

print("hello {}".format("world"))

输出：

hello world

+ 传入数字:`str.format()` 函数可以格式化数字，**在函数中传入数字，可以转换成指定的格式**。
+ 格式化字符串用双引号扩起来。
+ 大括号为固定格式，是字符串中需要替换的内容
+ 格式说明符前面通常有一个冒号，如果没有格式要求，可不添加冒号。
+ 格式说明符:
.2% 表示保留小数点后两位的百分比格式

<img width="1718" height="1508" alt="IMG_6815new" src="https://github.com/user-attachments/assets/70bd1234-ce0a-4f8f-b1ed-f6960b1eae38" />

```python
percent = "达成率{:.2%}".format(0.3333)
print(percent)
```

```python
# 列表numList
numList = [70000.0, 2232.0, 3401.0, 3381.0, 3198.0, 2793.0, 2488.0, 2403.0, 4307.0, 4563.0, 3453.0, 5890.0, 3341.0, 2145.0, 2476.0, 1839.0, 2549.0, 2688.0, 2399.0, 2054.0, 2011.0]
# 切片获取numList第二到最后一个元素，并进行sum()求和
# 赋值给变量total
total = sum(numList[1:])
# 将total除以列表numList第一个元素
# 使用format()将商转换成百分比格式，并赋值给percent
percent = "{:.2%}".format(total/numList[0])
# 使用print()输出percent
print(percent)
```

+ 从列表 saleData 中提取更新数据时，使用 int() 函数转换成整型
```python
# 导入xlrd模块
import xlrd
# 将Excel文件路径/Users/qian/每日数据.xlsx，赋值给变量path
path = "/Users/qian/每日数据.xlsx"
# 读取path的工作簿并赋值给变量data
data = xlrd.open_workbook(path)
# 使用sheets()获取工作表对象，索引第一个元素赋值给变量table
table = data.sheets()[0]
# 获取最后一行数据,并赋值给列表lastRow
lastRow = table.row_values(-1)
# 对列表lastRow进行切片，获取第二个到最后一个元素，并赋值给saleData
saleData = lastRow[1:]

# 读取工作表第2列非表头数据，赋值给变量allSales
allSales = table.col_values(1)[1:]
# 读取工作表第3列非表头数据，赋值给变量allUsers
allUsers = table.col_values(2)[1:]
# 读取工作表第4列非表头数据，赋值给变量salesVol
salesVol = table.col_values(3)[1:]

# 定义一个名为getResult(),设置参数dataList
def getResult(dataList):
    # 切片获取dataList第二个到最后一个元素
    # sum()函数计算列表元素的和，并赋值给total
    total = sum(dataList[1:])
    # 计算达成率，保留小数点后两位，并赋值给percent
    percent = "{:.2%}".format(total/dataList[0])
    # 返回percent
    return percent

# 按照模版格式化输出f"今天的销量为xx件，达成率为xx；\n新增用户为xx人，达成率为xx；\n销售额为xx元，达成率为xx。"
print(f"今天的销量为{int(saleData[0])}件，达成率为{getResult(allSales)}；\n新增用户为{int(saleData[1])}人，达成率为{getResult(allUsers)}；\n销售额为{int(saleData[2])}元，达成率为{getResult(salesVol)}。")
```

```python
# 导入pyautogui模块
import pyautogui
# 导入pyperclip模块
import pyperclip
# 导入time模块
import time
# 使用import导入requests模块
import requests
# 使用import导入json模块
import json
# 导入xlrd模块
import xlrd

# 定义一个openWechat()函数打开微信
def openWechat():
    # 使用hotkey()函数，操作按键"command", "e"
    pyautogui.hotkey("command", "e")
    # 停顿一秒
    time.sleep(1)

# 定义一个查询联系人chatWho()函数，参数为name
def chatWho(name):
    # 使用hotkey函数，操作按键"command","f"，打开搜索
    pyautogui.hotkey("command","f")
    # 停顿一秒
    time.sleep(1)
    # 使用pyperclip模块中的copy函数，复制联系人微信号到剪贴板
    pyperclip.copy(name)
    # 使用hotkey函数，操作按键"command", "v"，粘贴联系人微信号
    pyautogui.hotkey("command", "v")
    # 停顿一秒
    time.sleep(1)
    # 使用hotkey函数，操作按键"return"，确认搜索
    pyautogui.hotkey("return")
    # 停顿一秒
    time.sleep(1)
    
# 定义一个提交信息的sentMsg()函数，参数为msg
def sentMsg(msg):
    # 使用pyperclip模块中的copy函数，复制msg到剪贴板
    pyperclip.copy(msg)
    # 使用hotkey函数，操作按键"command", "v"，粘贴msg
    pyautogui.hotkey("command", "v")
    # 停顿一秒
    time.sleep(1)
    # 使用hotkey函数，操作按键"return"，确认搜发送
    pyautogui.hotkey("return")

# 定义一个getData()函数
def getData():
    # 将Excel文件路径/Users/qian/每日数据.xlsx，赋值给变量path
    path = "/Users/qian/每日数据.xlsx"
    # 读取path的工作簿并赋值给变量data
    data = xlrd.open_workbook(path)
    # 使用sheets()获取工作表对象，索引第一个元素赋值给变量table
    table = data.sheets()[0]
    # 获取最后一行数据,并赋值给列表lastRow
    lastRow = table.row_values(-1)
    # 对列表lastRow进行切片，获取第二个到最后一个元素，并赋值给saleData
    saleData = lastRow[1:]

    # 读取工作表第2列非表头数据，赋值给变量allSales
    allSales = table.col_values(1)[1:]
    # 读取工作表第3列非表头数据，赋值给变量allUsers
    allUsers = table.col_values(2)[1:]
    # 读取工作表第4列非表头数据，赋值给变量salesVol
    salesVol = table.col_values(3)[1:]

    # 定义一个名为getResult(),设置参数dataList
    def getResult(dataList):
        # 切片获取dataList第二个到最后一个元素
        # sum()函数计算列表元素的和，并赋值给total
        total = sum(dataList[1:])
        # 计算达成率，保留小数点后两位，并赋值给percent
        percent = "{:.2%}".format(total/dataList[0])
        # 返回percent
        return percent

    # 按照模版格式化输出f"今天的销量为xx件，达成率为xx；\n新增用户为xx人，达成率为xx；\n销售额为xx元，达成率为xx。"
    return f"今天的销量为{int(saleData[0])}件，达成率为{getResult(allSales)}；\n新增用户为{int(saleData[1])}人，达成率为{getResult(allUsers)}；\n销售额为{int(saleData[2])}元，达成率为{getResult(salesVol)}。"
    
# 调用openWechat()函数打开桌面微信
openWechat() 
# 调用chatWho(name)函数查找联系人"新品上线同步群"
chatWho("新品上线同步群")
# 调用sentMsg(msg)函数发送信息
# 信息内容为"大家好，每日数据同步"
sentMsg("大家好，每日数据同步")
# 调用sentMsg(msg)函数发送信息information
# 信息内容为getData()，赋值给information
information = getData()
sentMsg(information)
```
