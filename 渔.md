# 选择方式
我们可以通过【「需求的关键词」+ API 】进行搜索。

市面上有很多人工智能平台都提供了能实现类似功能的API。

例如，百度、腾讯、阿里、京东、科大讯飞、搜狗、网易等。

不同公司擅长的人工智能领域和提供的服务都有所不同。例如，讯飞更擅长语音处理，而旷视则会更擅长人脸识别。
# 如何选择API
## STEP1. 确认功能完整性

当我们点击页面上的链接进入到不同的AI平台进行浏览时，首先需要关注API的功能完整性，也就是这个API是否能满足我们的需求。

一般通过网页上的 【功能介绍】就能大概了解清楚。

不少平台也会提供【功能演示】，可以进一步确认输出的内容是否匹配我们的预期。

## STEP2. 评估技术文档
大致浏览了网页上的介绍后，我们就筛选掉了一些不能满足需求的API。

下一步，就是去 详细评估【技术文档】。

一个合格的技术文档可以帮助我们一目了然地了解如何调用接口，避免浪费时间和精力。
***
+ 如果决定通过SDK调用接口，那就需要评估相关编程语言的SDK文档，我们必须确保文档中至少有以下内容：

接口的能力介绍、请求接口的参数详情、返回数据的参数详情、相关的示例代码、以及请求接口时会遇到的报错情况说明。
***
+ 通过网络请求调用API

如果接口还未被封装到SDK里，那我们只能通过网络请求的方式来调用接口，也就需要评估API文档。

和评估SDK文档类似，我们得确保文档中至少有以下内容，且这些内容必须简单易懂：

接口的能力介绍、调用方式、相关的示例代码、以及请求接口时会遇到的报错情况说明。

## STEP3. 查询产品的免费额度和定价
筛选完功能的完整性并评估了技术文档后，我们就需要考虑到一个比较现实的问题了，那就是API的免费请求额度和付费服务。

无论是为了避免用户滥用还是为了赚钱，许多AI平台都会对API的调用有所限制，但又允许我们通过不同的付费计划来提升额度。

平台通常会限制接口的每秒调用次数、每天调用次数和总调用次数，我们需要管理好自己的请求频率来确保没有达到限制，否则继续调用就会出现报错。

有时平台也会设置接口的免费使用期限，比如只能在两年内免费使用，超出这个时间再请求接口也会报错

## STEP4. 评估平台提供的支持
当调用API出现问题时，比如关于API请求中断、请求出现超时等，这些只有平台相关的人员才能帮忙解答。

因此，我们需要关注AI平台是否提供技术支持。

有些平台除了提供技术专员/技术团队外，还会有交流论坛，我们也可以看看这些论坛是否活跃且有帮助。

## 安全提醒⚠️
无论最后我们选择了哪个平台的API，不管是通过网络请求接口还是直接调用SDK里分装好的接口，都会有对应的密钥来进行调用。

我们需要保护好自己的密钥，千万不要泄露出去！

# 调用API
1. 一是像之前课程里的一样使用封装好的SDK来调用；
2. 二是通过网络请求直接调用

+ 通过网络请求调用百度AI平台的API可分为三个步骤：

STEP1. 注册/登录百度帐号，并创建应用
STEP2. 使用Python调用接口
STEP3. 处理结果

+ 这里我们以图像去雾为例～

我们会先在本地【浏览器】采集一张图片，按照百度AI平台要求的规范格式，通过网络发送请求（Request）到AI平台。

平台接收到请求之后，会根据请求进行处理，再将数据分配到【服务器】中进行算法计算，获取结果。
***
+ 也就是说，需要我们操作的只有两个部分：
  + 发送请求调用接口和处理响应结果。
  + 接下来，我们会先来看看如何使用Python从浏览器向百度AI平台发送请求数据
     + 【服务器】会将最终获得的响应结果（Response），通过网络传送到本地【浏览器】。

+ 使用Python发送请求调用接口总共需要三个参数，拆分下来具体的步骤如下：

第一个参数request_url需要
1. 安装request模块
2. 获取Access Token
+ 如何获取Access Token？

百度AI平台贴心地在新手指南-鉴权认证机制中，为我们准备了几种常见编程语言的请求示例代码。

我们只需选择Python语言，然后点击右上角的「复制」按钮，将代码粘贴到本地，替换为自己的API Key和Secret Key即可。

+ requests.get()函数会向地址为host的服务器发送我们的API Key和Secret Key等信息，也就是发送一个验证命令

百度的后台接收到验证命令后，就会对信息进行验证，从而将验证的结果返回回来
```python
# encoding:utf-8

# 导入requests模块
import requests 

# client_id 为官网获取的API Key
# client_secret 为官网获取的Secret Key
host = 'https://aip.baidubce.com/oauth/2.0/token?grant_type=client_credentials&client_id=u9PfhKsAZf5jPYZN1lHQSDQQ&client_secret=lCpinlOL7YPffoIey25Kmqlob8d25DQQ'

# 使用requests.get()函数，向服务器传递数据
response = requests.get(host)

# 如果传递成功
if response:
    # 输出返回的结果
    print(response.json())

# 获取Access Token，并存储在变量access_token中
access_token = result["access_token"]

# 输出access_token
print(access_token)
```
4. 获取request_url
```python
# 将文档中提供的请求URL赋值给变量request_url
request_url = "https://aip.baidubce.com/rest/2.0/image-process/v1/dehaze"

# 获取完整的request_url
request_url = request_url + "?access_token=" + access_token

# 输出request_url
print(request_url)
```
<img width="1150" height="587" alt="25-24 (2)" src="https://github.com/user-attachments/assets/4c8482ec-19d5-4c43-9908-0b34ada8b293" />


### 第二个参数data需要
4. 转换图片格式
+ 第二个参数data表示发送请求的数据
+ 每个人的图片格式都是各种各样的，使用我们需要把图片转换为base64编码，这样百度AI平台才能识别，从而进行解析。
```python
'''获取第二个参数data'''
# 导入base64模块
import base64
# 打开图片文件
f = open('/User/img/pic.png', 'rb')
# 把图片转换为base64编码
img = base64.b64encode(f.read())
# 将第二个参数以字典的形式存储在params中
params = {"image":img}
```
  
### 第三个参数headers需要
5. 获取headers
+ 第三个参数headers是一个定值，直接复制粘贴就好。
<img width="825" height="584" alt="25-31" src="https://github.com/user-attachments/assets/599983e8-09d4-440b-aa92-2d7507bb479f" />

```python
'''获取第三个参数headers'''
# 将相关内容赋值给headers
headers = {'content-type': 'application/x-www-form-urlencoded'}
```

## 发送请求
+ 根据官方文档的示例代码，我们现在就可以把这些信息通过requests.post()函数发送出去。

这样百度AI平台的后台收到我们的信息之后，就会进行算法处理。
```python
'''发送请求'''
# 使用requests.post()发送请求
response = requests.post(request_url, data=params, headers=headers)
```

## 获取返回结果
和之前获取Access Token类似，我们先使用if语句判断是否请求是否成功。

如果请求成功，就通过response.json()，将返回的结果存储在result变量里。

```python
'''获取返回结果'''
# 如果传递成功
if response:
    # 将返回的结果存储在result变量里
    result = response.json()
```

在实际使用中，调用接口后往往会返回很多信息，而我们只会用到其中的一些，所以需要根据自己的需求去进行选择。

很显然，我们只需要通过result["image"]获取处理后图片的base64编码即可，这就是我们想要的内容。

```python
'''获取base64编码图片'''
# 获取图片的base64编码
img_str = result["image"]
```

## base64编码转换为图片
+ 首先，我们需要使用base64.b64decode()函数，对获取到的处理后图片的Base64编码进行解码
+ 接下来，就可以使用with...as...语句配合open()函数的方式，以"wb"的形式写入，创建路径为'/User/img/dehaze.png'的新文件。
+ 最后，利用file的write()方法将解码后的img_data写入到文件中即可。

```python
'''将base64编码转换为图片'''
# 使用base64.b64decode()进行解码
img_data = base64.b64decode(img_str)

# 以wb的方式新建文件
with open('/User/img/dehaze.png', 'wb') as f:
    # 将解码后的图片写入到指定路径下的文件中
    f.write(img_data)
```
