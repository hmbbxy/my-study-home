# 语音合成
+ 定义

语音合成简称TTS技术（Text to Speech），它是将任意文本转换成语音的技术。

+ TTS技术是人机进行语音交互必不可少的模块。
+ 隐喻:
TTS技术就好比人类的嘴巴，能够将获得的文字朗读出来。

***
如果说语音识别技术是让机器学会“听”话，那么语音合成技术就是让机器学会"说"话。

语音识别的目的：将输入的语音转换成文字

语音合成的目的：将输入的文字转换成语音
***
# 语言识别
+ 创建语音识别SDK客户端
```python
# 使用from...import从aip中导入AipSpeech
from aip import AipSpeech

# 存储访问密钥信息，包括客户端ID、API接口验证序号和API接口密钥
APP_ID = "10252021"
API_KEY = "ZHe7788sh11GEjIAdEKeY"
SECRET_KEY = "JMMzHe7788BUSH1ZhEnM1YUEhh"

# 传入密钥创建AipSpeech客户端，并将结果存储在client中 
client = AipSpeech(APP_ID, API_KEY, SECRET_KEY)

# 输出查看创建的client对象
print(client)
```
+ 定义语音识别接口调用函数
+ 读取并处理音频文件
+ 调用语音识别接口调用函数并输出结果
```python
"""创建语音识别客户端"""
from aip import AipSpeech
APP_ID = "10252021"
API_KEY = "ZHe7788sh11GEjIAdEKeY"
SECRET_KEY = "JMMzHe7788BUSH1ZhEnM1YUEhh" 
client = AipSpeech(APP_ID, API_KEY, SECRET_KEY)

"""语音识别接口调用函数"""
def audio2text(wav):
    rejson = client.asr(wav,"wav",16000,{"dev_pid": 1537})
    if rejson["err_no"] == 0:
        msg = rejson["result"][0]
    else:
        msg = "语音识别错误！"
    return msg

"""音频文件参数设置"""
from pydub import AudioSegment
sound=AudioSegment.from_wav("/Users/yequ/音频.wav")
sound=sound.set_frame_rate(16000)
sound=sound.set_channels(1)

"""导出参数设置后音频"""
path = "/Users/yequ/参数设置后音频.wav"
sound.export(path,format="wav")

"""读取参数设置后音频"""
with open(path, 'rb') as fp:
    wav = fp.read()

"""调用语音识别接口函数并输出结果"""
text = audio2text(wav)
print(text)
```
# 文字转语音
part1. 获取二进制数据流

part2. 导出并保存音频文件
## 调用语音合成接口
```python
"""创建语音识别和语音合成客户端"""
from aip import AipSpeech
APP_ID = "10252021"
API_KEY = "ZHe7788sh11GEjIAdEKeY"
SECRET_KEY = "JMMzHe7788BUSH1ZhEnM1YUEhh" 
client = AipSpeech(APP_ID, API_KEY, SECRET_KEY)

"""语音识别接口调用函数"""
def audio2text(wav):
    rejson = client.asr(wav,"wav",16000,{"dev_pid": 1537})
    if rejson["err_no"] == 0:
        msg = rejson["result"][0]
    else:
        msg = "语音识别错误！"
    return msg

...

"""调用语音识别接口函数并输出结果"""
text = audio2text(wav)

# 调用语音合成接口把结果赋值给rejson变量
rejson = client.synthesis(text, "zh", "1",{"vol": 12,"spd": 6,"pit": 7,"per": 4})
```
+ 语音合成接口

对创建的AipSpeech客户端，使用synthesis()函数调用语音合成接口。
+ 待合成文字

必选参数，text为字符串类型的文字信息，文字长度必须小于1024字节。

若文字长度超过1024字节，可以分为多段文本进行合成
+ 语音模式

必选参数，传入字符串"zh"，设置合成语音模式为中英文。

注意：该参数为百度API设定的固定值，不支持修改。也就是说，该接口只支持中文、英文或者中英文混合的语音合成。
+ 客户端类型

必选参数，传入字符串"1"，设置客户端类型为web端。

注意：该参数为百度API设定的固定值，不支持修改
+ 合成语音参数

可选参数，传入**字典类型**数据，设置合成语音参数。

通过调整参数数值，可以呈现不同的合成语音效果。
  + 音量

   可选参数，vol表示合成语音音量，取值范围为0-15。

   默认值为5，表示中音量（取值为0时为音量最小值，并非为无声）。
   + 语速

   可选参数，spd表示合成语音语速，取值范围为0-15。

   默认值为5，表示中语速（取值为0时为语速最小值）
   + 音调

   可选参数，pit表示合成语音音调，取值范围为0-15。

   默认值为5，表示中音调（取值为0时为音调最小值，并非为无音调）。
   + 发音人

   可选参数，per表示合成语音发音人，共有以下四种发音人可选：

   度小美=0，度小宇=1，度逍遥（基础）=3，度丫丫=4

+ 利用语音合成接口，将文字转为语音，返回值为二进制数据流。
+ 当调用语音合成接口失败时，返回结果为一个字典类型数据。
其中，以下两个参数用于错误信息描述：

1. err_no，错误码
2. err_msg，错误码描述

<img width="1804" height="1010" alt="P26" src="https://github.com/user-attachments/assets/5ca9bdd5-d306-466a-b7df-bea7e40ee9e8" />

## 正确返回与错误返回

将语音合成部分封装为函数text2audio()，传入参数wordText。

由于语音合成错误返回时的数据类型为字典，通过if语句判断：

若返回值result的数据类型是dict(字典类型)，则返回“语音合成错误”的提示信息，否则返回result。

判定数据类型时，通过type()函数判定。
```python
"""创建语音识别客户端"""
from aip import AipSpeech
APP_ID = "10252021"
API_KEY = "ZHe7788sh11GEjIAdEKeY"
SECRET_KEY = "JMMzHe7788BUSH1ZhEnM1YUEhh" 
client = AipSpeech(APP_ID, API_KEY, SECRET_KEY)

"""语音识别接口调用函数"""
def audio2text(wav):
    rejson = client.asr(wav,"wav",16000,{"dev_pid": 1537})
    if rejson["err_no"] == 0:
        msg = rejson["result"][0]
    else:
        msg = "语音识别错误！"
    return msg

"""音频文件参数设置"""
from pydub import AudioSegment
sound=AudioSegment.from_wav("/Users/yequ/音频.wav")
sound=sound.set_frame_rate(16000)
sound=sound.set_channels(1)

"""导出参数设置后音频"""
path = "/Users/yequ/参数设置后音频.wav"
sound.export(path,format="wav")

"""读取参数设置后音频"""
with open(path, 'rb') as fp:
    wav = fp.read()

"""调用语音识别接口函数并输出结果"""
text = audio2text(wav)

# 定义语音合成接口调用函数text2audio()，传入参数wordText
def text2audio(wordText):
    # 调用语音合成接口把结果赋值给result变量
    result = client.synthesis(wordText, "zh", "1",{"vol": 12,"spd": 6,"pit": 7,"per": 4})
    # if语句判断，若返回值result的数据类型是dict(字典类型)
    if type(result) == dict:
        # "语音合成错误"字符串赋值给output
        output = "语音合成错误"
    # 否则
    else:
        # result赋值给output
        output = result
    # 函数返回output
    return output

# 传入参数text调用text2audio()函数，并输出查看结果
print(text2audio(text))
```

# 导出并保存音频文件
```python
# 使用with...as以wb方式，打开路径为/Users/yequ/音频变音版.wav的文件并赋值给fp
with open("/Users/yequ/音频变音版.wav", "wb") as fp:
    # 使用write()将audio写入fp
    fp.write(audio)
# 输出提示信息"变音文件生成完毕！"
print("变音文件生成完毕！")
```
